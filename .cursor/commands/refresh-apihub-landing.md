# Refresh APIHUB landing content

Follow the project skill **qubership-apihub-landing-refresh** (`.cursor/skills/qubership-apihub-landing-refresh/SKILL.md`): use the public GitHub releases and docs to update `sites/apihub/` (especially `src/data/content.ts`, release and use-case markdown under `src/content/`), then run `npm run build` in `sites/apihub/`.
